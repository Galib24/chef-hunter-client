import React from 'react';
import Header from '../Header/Header';

const Conditions = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Conditions;